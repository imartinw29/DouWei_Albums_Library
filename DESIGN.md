# DouWei Archive — Design System（设计宪法）

> 本文件是 DouWei Albums Library 的视觉规则。任何页面改动（含 Agent 代改）不得违背。
> 原则一句话：**不替窦唯说话，只替他的作品留下位置。**

## 0. 定位

这不是"窦唯个人资料库"，是一座可以漫游的**窦唯作品档案馆**（unofficial fan archive）。
不做：生平/婚姻/黑豹/王菲/魔岩三杰/名人评价。那些互联网已有无数处，这里不重复。

## 1. 颜色

| 角色 | 值 | 说明 |
|---|---|---|
| 底 | `#0a0a0a` 附近（近黑，带一点暖） | 不是纯黑 `#000` |
| 纸 | 米白 `#e6ddcc` 系 | 旧印刷/铅笔白，用于文字 |
| 灰阶 | 多级灰（dim/mid/soft） | 照片不强行调色，原图即原图 |
| 强调色 | **不设固定 accent** | 颜色由作品带进来：某张封面是紫的，页面那一刻出现一点紫 |

> ⚠️ 现有代码里的 `--accent: #b08c63`（暗金）是历史遗留，逐步收敛；禁止新增"主题色"。

## 2. 字体

- 中文：思源宋体 / Noto Serif SC（克制衬线），标题可用细字重
- 英文：干净无衬线（Inter / Helvetica Neue）
- **禁用**：毛笔字、书法字库、水墨风字体、印章风
- 字号层级：**信息越重要字越大，越不重要越小**（专辑名 > 年份 > 描述 > 来源），不全部同字号

## 3. 网格

- 隐形网格（8/12 列）做骨架
- **允许不对称、错位、破格**——这是"层次感"的来源
- 禁止：所有卡片统一对齐的"数据库感"

## 4. 留白

- 首页可以非常空（大标题 + 一句年份，往下滚）
- 区块间距大（clamp 3-6rem），元素间距克制
- 少装饰、少动画、少标签、少"设计感"；大留白、强秩序、有节奏、有时间感

## 5. 卡片（专辑）

- 封面是主体，文字极少（专辑名 + 年份）
- hover：封面微亮（brightness 1.05），不做位移浮动/发光堆叠
- 圆角极小（≤2px）或无；无渐变边框

## 6. 时间流（timeline 视图）

- 按年份分组，年份是"节点"不是"标签"
- 封面错落排列（交替偏移/不同大小），有节奏
- 让用户自己发现创作轨迹，不替用户下结论

## 7. Cover Wall（封面墙）

- 全屏封面档案墙，按年份密度排列
- hover 显示专辑名 + 年份，点击进详情
- 感觉是"在翻一整面唱片墙"

## 8. Fragments（照片/碎片页，待建）

- 照片不配解释，最多一行：`1998 Beijing` 或干脆只有年份
- 每张照片保留来源（source: 互联网公开资料，版权归原作者）

## 9. 动效

- 克制：transition 0.2-0.3s ease
- 无弹跳、无缩放入场、无滚动视差
- Fragments 随机切换可以有一个"翻牌"感

## 10. 技术约束（沿用现有）

- 纯静态 GitHub Pages，无框架
- 数据唯一源 `albums.json`，页面只读它
- JS 外部文件 + defer；卡片用 `data-album` + 事件委托，不用 inline onclick
- 封面 `loading="lazy" decoding="async"`
- 小步 patch，禁止整页重写；改完本地 server（45000+ 端口）验证再 push
